<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h1><%= @evento.nombre %></h1>
      <p><%= @evento.descripcion %></p>
      <%= link_to 'Reservar', reservar_evento_path(@evento), class: "btn btn-primary" %>
    </div>
  </div>
</div>